import 'package:flutter/material.dart';


class InvalidUser extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.grey,
        appBar: AppBar(
          title: Text("InvalidUser"),
        ),
    );
  }
}